# CheatSheet

```javascript
import { initRequest, Biu, makeCustomBiutor, axios  } from '@we-mobius/mobius-js'
import { initConfig, makeConfigObserver, configObservables, getConfig } from '@we-mobius/mobius-js'
import { initAuth, authObservers, authObservables } from '@we-mobius/mobius-js'
import { initTheme, makeThemeObserver, themeObservables } from '@we-mobius/mobius-js'
import { initDevice, deviceObservables } from '@we-mobius/mobius-js'
```
