# CheatSheet

```javascript
import { initRequest, Biu, makeCustomBiutor, axios  } from '@we-mobius/mobius-services'
import { initConfig, makeConfigObserver, configObservables, getConfig } from '@we-mobius/mobius-services'
import { initAuth, authObservers, authObservables } from '@we-mobius/mobius-services'
import { initTheme, makeThemeObserver, themeObservables } from '@we-mobius/mobius-services'
import { initDevice, deviceObservables } from '@we-mobius/mobius-services'
```
